# Optional: enables LLM map generation. Without a key the server uses the
# deterministic procedural generator (same schema — the client can't tell).
OPENAI_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_BASE_URL=https://api.openai.com/v1
PORT=3001
